# 蓝洞婚礼宾客管理后台

后台地址为 `/admin/`。它提供密码登录、宾客姓名/电话/留言搜索、住宿筛选、统计卡片和 Excel（`.xlsx`）导出。

## 先查看界面

本地或部署后的演示地址：`/admin/?demo=1`。

演示数据只存在于当前浏览器，不会读取、提交或覆盖任何真实宾客资料。确认视觉与交互时可直接使用。

## 正式启用前要做的 4 件事

1. 在 Cloudflare 创建一个 D1 数据库，例如 `blue-hole-wedding-rsvps`。
2. 执行 [migrations/0001_create_rsvps.sql](migrations/0001_create_rsvps.sql) 创建登记、限流与登录保护数据表。
3. 在 Pages 项目「Settings → Bindings」添加 D1 绑定，变量名必须是 `DB`。
4. 在同一项目「Settings → Environment variables」添加两个**加密变量**，不要写进 GitHub：
   - `ADMIN_PASSWORD`：后台登录密码。
   - `SESSION_SECRET`：一段随机、足够长的字符串，用于签名 8 小时登录会话。

部署完成后，访问 `/admin/` 输入 `ADMIN_PASSWORD` 即可查看真实数据。管理后台接口带有 HttpOnly Cookie、密码比对保护和登录限流；宾客手机号、留言不会被打包进前端页面。

## 与现有请柬的衔接方式

现在的线上请柬将 `/api/rsvp` 转发到原有登记服务。为了避免在你确认前影响任何真实登记，本项目保留了这一行为：

- **尚未绑定 `DB`**：请柬继续使用原有登记服务，后台会提示“尚未完成安全配置”。
- **绑定 `DB` 且执行迁移后**：新的 RSVP 会自动保存到 D1，后台随即可以查看和导出。

注意：切换到 D1 前已经留存在原服务里的历史登记，不会自动复制过去。若希望历史宾客也显示在新后台，请先从原服务导出数据；我可以再补一个仅管理员可用的一次性导入工具，避免手工逐条录入。

## Excel 导出说明

点击「导出 Excel」会在当前浏览器本地生成 `.xlsx` 文件，字段包括姓名、赴约人数、住宿、电话、留言、提交时间和最后更新时间。数据不会上传给 Excel 服务；首次导出时会加载公开的表格生成库，因此需要正常联网。

